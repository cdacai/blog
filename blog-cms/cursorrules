第一，处理又拍云的图片删除、修改、查询、移动、相册创建、删除，后端不要使用RESTful API，使用restManager sdk来处理。
第二，上传图片时候的压缩，使用前端压缩，后端不压缩。同时，前端在md编辑器里的图片上传功能，需要压缩，相册管理里的图片上传功能，不需要压缩。
第三，图片压缩的逻辑要复用同一个方法，不要重复写：即，压缩限度是最大不能超过150kb，宽度和高度不能超过1000px。
第四，后台管理前端部分，在请求接口时候，由于公共请求方法已经有了admin前缀，所以，在请求接口时候，不要使用admin前缀。
